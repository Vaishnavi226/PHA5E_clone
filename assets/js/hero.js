const floatingImg = document.querySelectorAll('.floatingImg');

floatingImg.forEach((img, index)=>{

    // change on hovering 
    img.addEventListener("mouseover",()=>{
        // 1. main text will go behind
        //2. image text should be visible 
    });

    // seeting back chnages on removeing mouse from hover state
    img.addEventListener("mouseleave",()=>{

    });
})













